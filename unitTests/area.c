#include <stdio.h>

int main() {
    // Default input value
    int side = 5;

    // Calculate area
    int area = side * side;

    // Print the result
    printf("Area of the square with side length %d is: %d\n", side, area);

    return 0;
}